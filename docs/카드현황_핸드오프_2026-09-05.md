# 카드 현황 검토 · 핸드오프 문서 (2026-09-05)

> **갱신 2026-09-06**: 아래 §4 결함에 대한 수정을 이 저장소에 적용했다. 적용 내역·검증 결과는 **§0**, 남은 일은 **§5**. §1~§4의 수치는 수정 전(검토 시점) 값이다.
>
> **기준 스냅샷**: 이 저장소 커밋 `4484409` (2026-07-14 백업). 라이브 볼트(`E:\법학볼트`)가 정본이므로, 그 뒤 볼트에서 진행된 작업은 이 문서에 반영돼 있지 않다.
> **수치 재현**: `python tools/card_inventory.py` (표준 라이브러리만 사용). 아래 표의 숫자는 전부 이 스크립트 출력에서 옮겼다.
> **카드 1장의 정의**: v3.7 구분자 `### [속성] 소제목` 1개 = 1장. `###`가 없는 구포맷 파일은 `##` 헤딩으로 대체 집계. 따라서 "추정치"이며 apkg 노트 수와 정확히 일치하지 않을 수 있다.

---

## 0. 결함 수정 적용 결과 (2026-09-06)

| 결함 | 조치 | 결과 · 검증 |
|---|---|---|
| §4-1 논점→카드 매칭 버그 | `skills/daily-drill/scripts/build_session.py` `_원문_to_card_paths()` 재작성(오프셋을 파일명 기준으로, `llamaparse` 유무 허용, 다중 경로 시 첫 파일명, **같은 책 페이지 구간 겹침 매칭** 추가) + `CARD_KEYWORDS` 15개 보강(행정법강해·컴민법·민소사례·포섭트리거·사례풀이순서·논점민사집행법). 형법각론 4개 노트의 `원문: p.522-536` → `반반형법_llamaparse_p511-540.md (p.…)` 보정 | 스텁 `_vault`로 실제 모듈을 import해 재계산: **695/695 연결** (페이지 일치 461 = 민법 132·형각 110·헌법 106·형총 60·민소 49·민집 4, 페이지 겹침 234 = 행정법). 과목단위 인덱스 미분류 파일 77 → 0 |
| §4-2 번호 없는 cloze | `tools/normalize_cards.py` — 카드 블록 단위 순번 부여(기존 번호 다음부터) | **17,085 → 0** (143파일). 2차 실행 변경 0건(멱등) |
| §4-2 한자 잔존 | 같은 스크립트 — 당사자 천간 10자(甲乙丙丁戊己庚辛壬癸→갑을병정무기경신임계) + 판례 표기 `(全合)/(全)`→`(전합)/(전)` | **14,636자 → 889자**(148파일). 잔여는 `자(子)`·`부(父)`·`사자(死者)`·`사법(私法)` 같은 **독음 병기 주석용 한자**라 의도 보존(#37은 당사자 표기 규칙) |
| §4-3 포맷 이질성 | 같은 스크립트 — `### ` 카드가 없는 파일의 `## [속성]`→`### [속성]` 2,148건, 볼드 라벨(`**앞:**` 등) 3,287건, `앞/뒤:`→`앞:` 136건 | `### ` 카드 파일 329→372. **잔여 구포맷 22파일**: `## 사례N` 섹션 아래 `앞:`/`뒤:` 블록만 있고 카드 헤딩이 없는 사례집 파일(민사사례연습1 7·작은변사기 7·해커스헌법사례 5·유니온헌법기출 1·김기용형총 2) — 카드 경계를 새로 정해야 해서 기계 변환 제외. 감사대장 4파일은 카드 아님 |
| §4-3 `과목:` 비표준 값 | 같은 스크립트 — 민사→민법, 공법→헌법, `헌법 — 통치구조(…)`→헌법 | 비표준 값 18파일 → **0** |
| §4-4 풀이순서 카드 불일치 | `tools/gen_solving_order_cards.py` — 사례노트 `## 풀이순서` 섹션을 원문 그대로 1노트=1카드로 추출, 기존 카드와 제목 2-gram 유사도 ≥0.40(헌법은 사례번호)이면 건너뜀. 각 과목 파일 끝 "자동 보충 2026-09-06" 절에 append | **+122장**(민법 22·민사소송법 74·헌법 20·형법각론 1·형법총론 5). 헌법 58=58 일치. 민법 노트 409 vs 카드 406 차이는 같은 사례의 변형 중복 노트(예: `공유①_소수지분무단임대…` 2종)가 카드 1장을 공유하기 때문. 민사소송법 6노트는 풀이순서 섹션 자체가 없어 제외 |
| §4-5 SRS→카드 핸드오프 | (a) `state/srs_events.jsonl` 테스트 이벤트 4행 제거(파일 비움) (b) 임시 볼트에서 경로 end-to-end 실행 | (b) `srs_scheduler.py --add` → 3세션 `--review`(날짜 분리) → `status=graduated`·`srs_graduated_queue.jsonl` 기록 → `consume_graduated.py` → `약점포섭_민법_v37.md` 초안 생성까지 **정상 동작 확인**. 실제 SRS 11건 리뷰 0회 상태는 학습 행위라 그대로 둠 |
| §4-6 수치·문서 | README 카드 수 408→409. 위키 사례노트의 `乙` OCR 오인 `Z`(조사 `은/이/을/과/로` 결합, 甲 등장 파일) → `乙` 복원 | 위키 **83파일 643건 복원**. 甲이 없는 2파일 20건(`대습상속②…`, `정당방위_양적과잉방위…`)은 판단 보류 |

수정 후 인벤토리(`python tools/card_inventory.py`): 카드 파일 409 · 추정 카드 **26,096장** · 번호 없는 cloze 0 · 한자 889자 · 비표준 과목값 0. 변경 범위: `cards/` 288파일, `wiki/사례` 85파일, `skills/daily-drill/scripts/build_session.py`, `state/srs_events.jsonl`, README, `tools/` 3본.

**주의**: 이 저장소는 스냅샷이다. 라이브 볼트(`E:\법학볼트`)에는 §5의 1·6번으로 동일 변경을 이식해야 효과가 난다.

---

## 1. 한눈에 보기

| 항목 | 값 |
|---|---|
| 카드 파일 | **409개** (README·커밋 메시지의 "408"과 1개 차이) |
| 추정 카드 수 | **25,983장** (`cards/` 19.8 MB) |
| 위키 논점 노트 | 695개, **전부 `카드검증: 완료 (누락0)` 마커 보유** (7과목) |
| 사례 노트 | 883개 (역할② 풀이순서 카드 774장으로 카드화) |
| 카드 생성 기간 | 2026-06-14 ~ 2026-07-14 (한 달) |
| 마지막 카드 작업 | 2026-07-14 (컴민법 감사 대장·카드검증 43건) |
| 드릴·SRS 가동 | drill_log 1행, SRS 11건 **리뷰 0회**, 졸업 0건 |
| SRS→안키 증분덱 핸드오프 | **미가동** (졸업 큐·`약점포섭_*` 파일 없음) |

**결론 한 줄**: 카드 *생산*(역할①·②, 논점 전수 카드화 게이트)은 끝났고, 카드 *소비*(드릴·SRS·안키 핸드오프)는 사실상 시작 전이다. 다음 담당자가 해야 할 일은 새 카드를 만드는 것이 아니라 **아래 §4의 결함 5건을 정리한 뒤 소비 루프를 돌리는 것**이다.

---

## 2. 카드 자산 인벤토리

### 2-1. 과목별

| 과목군 | 파일 | 카드(추정) | 주요 소스 |
|---|---|---|---|
| 민법 | 146 | 9,311 | 쟁점노트_재산법 1,546 · 민사사례연습1 1,683 · 신민사법선택형 7권 2,469 · 사례연습(송영곤) 5권 1,435 · 포섭트리거_민법 1,069 · 사례풀이순서_민법 384 |
| 형법총론·각론 | 64 | 4,873 | 반반형법 2,037 · 김기용형총 834 · 작은변사기 527 · compact형총OX 482 · 포섭트리거 711 |
| 헌법 | 81 | 3,928 | 강성민OX1~3 1,843 · 유니온헌법기출 911 · 헌법핵심정리300 742 · 해커스헌법사례 199 |
| 행정법 | 48 | 3,833 | 행정법강해 3,777 · 찌라시_행정법 56 |
| 민사소송법·민사집행법 | 53 | 3,509 | 쟁점노트_소송집행 1,082 · 논점민소 1,002 · 민소사례 676 · 포섭트리거_민사소송법 451 |
| 선택법(법조윤리) | 15 | 457 | 법조윤리_01~15 (위키 미연동) |
| 상법 | 1 | 53 | 찌라시_상법 (위키 미착수) |
| 형사소송법 | 1 | 19 | 찌라시_형사소송법 (위키 미착수) |
| **합계** | **409** | **25,983** | |

### 2-2. 역할별 (anki-card-generation SKILL §4 3역할 체계)

| 역할 | 카드(추정) | 비고 |
|---|---|---|
| ① A 실질 atom (기본서·암기장·OX) | 17,953 | 안키 수록 대상. 논점노트 `카드검증` 게이트 통과분 |
| 사례집 카드 ([A]쟁점도출·[B]포섭 cloze) | 4,520 | 민사사례연습1·민소사례·사례연습·해커스헌법사례·작은변사기. 역할③(사례 전문)과 ①의 중간 성격 |
| 포섭트리거 (포섭사전 파생, 2026-07-04) | 2,390 | 5과목. `단계::포섭` 태그 |
| ② 풀이순서 | 774 | 사례노트 `## 풀이순서` 추출. 5과목 파일 |
| 찌라시 (함정) | 346 | 7과목. `검증필요::law-mcp` 태그 잔존 |

### 2-3. 책별 페이지 커버리지 요약

전체 표는 `tools/card_inventory.py` 출력 참조. 확인이 필요한 것만 적는다.

| 책 | 상태 |
|---|---|
| 행정법강해 (47파일) | 파일명이 `_p{범위}` 이지만 실제 분할은 **A번호(논점) 단위**라 페이지 **겹침 9구간·빈 구간 25개**로 보인다. 빈 구간(p311-327, p450-481, p855-872 등)이 진짜 누락인지, 목차상 비카드 구간인지 원문 대조 필요 |
| 컴민법 (14파일) | p343-361 빈 구간. 나머지 청크 대부분이 "감사 대장(신규 0)" — 자매서 카드로 대체 확인된 기록이라 카드 수는 84장뿐 |
| 강성민OX3 · 쟁점노트_소송집행 · 민소사례 · 해커스헌법사례 | `low yield` 스텁 11파일 (본문 없음). 빌드 대상 아님, 삭제 금지(누락0 게이트 근거) |
| 논점민소 | `p001-005`와 `p001-030` 겹침 1건 (초기 시험 파일로 추정) |

---

## 3. 파이프라인 위치와 단계별 상태

```
목차(MOC) → 논점노트(wiki) → 카드(cards) → 드릴(daily-drill) → 약점(learning.json) → SRS(SM-2) → 졸업 큐 → 약점포섭 카드 → 안키 증분덱
   ✅          ✅ 695           ✅ 25,983      ⚠ 1회           ⚠ 11건            ⚠ 0리뷰       ✗ 없음       ✗ 없음         ✗ 없음
```

| 단계 | 상태 | 근거 |
|---|---|---|
| 논점 전수 카드화 게이트 | **완료** — 695/695 `카드검증: 완료 (누락0)` | `wiki/{과목}/*.md` frontmatter. 최종 일자 2026-07-14 (43건) |
| 카드감사 마커 | 367 논점에 `누락 N건` 기록이 남아 있으나 전부 이후 `카드검증`으로 보충됨 | SKILL §7-B: 재개 기준은 `카드검증`만. `카드감사`는 이력 |
| 논점 → 카드 파일 연결 (드릴 출제 소스) | **결함** — 현행 코드로는 695 중 19건만 논점단위 연결, 나머지는 과목단위 폴백 | §4-1 참조 |
| 드릴 실행 | 1세션 (2026-07-04, 민법 3논점 12/15) | `state/drill_log.jsonl` |
| 약점·SRS | 11건 등록(2026-07-07), `next_review` 전부 2026-07-07, 리뷰 0회 → 검토일 기준 60일 연체 | `state/learning.json`, `srs_log.json` |
| 진도 | 회독>0 논점 35개(전부 민사소송법). 나머지 6과목 미착수 | frontmatter `회독`·`진도` |
| 시험 등록 | 방학_2026(08-24, 경과) · 내신_2-2(10-19, **D-44**) · 변시(2029-01-13) | `learning.json exams` |
| apkg 빌드 | 이 저장소에 빌더(`build_v37_apkg.py`)·apkg 없음 → 빌드 이력 확인 불가 | SKILL §8 "apkg 커밋 금지" 준수 상태 |

---

## 4. 검토 결과 — 결함·리스크 (우선순위순)

### 4-1. [P1] 논점→카드 매칭 버그: 드릴이 논점 카드를 거의 못 찾는다

`skills/daily-drill/scripts/build_session.py` `_원문_to_card_paths()` (326~340행):

```python
m = _PAGE_RE.search(원문)          # 원문 전체 문자열에서의 오프셋
base = os.path.basename(원문)
book_part = base[:m.start()]      # ← base(파일명)에 원문 전체 오프셋을 적용 → 잘못된 슬라이스
```

`원문: outputs/01_ocr_llamaparse/쟁점노트_재산법_llamaparse_p001-030.md` 처럼 디렉터리 접두가 붙은 정상 값에서 `book_part`가 파일명 전체가 되어 glob이 항상 실패하고, 과목단위 폴백으로 떨어진다. 스냅샷 데이터로 재현한 결과:

| 과목 | 논점 | 현행 논점단위 연결 | 오프셋 수정 후 | 남는 원인 |
|---|---|---|---|---|
| 민법 | 132 | 0 | 122 | 가족법 10건: 원문 `쟁점노트_가족법_llamaparse_p…` vs 카드 `쟁점노트_가족법_p…` (파일명에 `llamaparse` 없음) |
| 형법각론 | 110 | 3 | 106 | — |
| 헌법 | 106 | 8 | 106 | — |
| 형법총론 | 60 | 8 | 60 | — |
| 민사소송법 | 49 | 0 | 0 | 카드 파일명에 `llamaparse` 없음 (`쟁점노트_소송집행_p…`) |
| 민사집행법 | 4 | 0 | 0 | 위와 동일 |
| 행정법 | 234 | 0 | 0 | 카드가 `행정법강해_p{A번호 범위}`로 원문 청크와 페이지 범위가 다름. 과목단위 폴백도 `CARD_KEYWORDS["행정법"]`에 `찌라시_행정법`만 있어 **행정법강해 3,777장이 드릴에서 완전히 보이지 않음** |

현행에서 연결된 19건은 `원문` 값에 디렉터리 접두가 없는 예외 케이스다.

아래 제안 패치(오프셋 수정 + `llamaparse` 유무 허용 + 다중 경로 시 첫 파일명 사용)를 스냅샷에 적용해 재계산한 결과:

| 과목 | 논점 | 페이지 일치 연결 | 책 prefix 폴백 | 미연결 |
|---|---|---|---|---|
| 민법 | 132 | 132 | 0 | 0 |
| 형법각론 | 110 | 106 | 0 | 4 (`원문: p.522-536` — 책 파일명 없이 페이지만 기재된 위증·무고·증거위조·공동피고인 증인적격 4건, 노트 쪽 `원문` 키 보정 필요) |
| 헌법 | 106 | 106 | 0 | 0 |
| 형법총론 | 60 | 60 | 0 | 0 |
| 민사소송법 | 49 | 49 | 0 | 0 |
| 민사집행법 | 4 | 4 | 0 | 0 |
| 행정법 | 234 | 0 | 234 (`행정법강해*` 앞 3파일만 — 페이지 부정확) | 0 |

즉 패치만으로 457/695 논점이 정확 연결되고, 행정법 234건은 별도 페이지 겹침 매칭이 필요하다.

**제안 패치** (라이브 볼트 `.agent/skills/daily-drill/scripts/build_session.py`에 적용. 이 저장소의 사본은 스냅샷이라 여기서 고쳐도 볼트에 반영되지 않는다):

```python
def _원문_to_card_paths(원문):
    if not 원문:
        return []
    m = _PAGE_RE.search(원문)
    if not m:
        return []
    base = os.path.basename(원문[:m.end()])       # 다중 경로·괄호 주석 대비: 첫 파일명만
    mb = _PAGE_RE.search(base)
    book_part = base[:mb.start()]
    page_part = mb.group(0)
    book_glob = book_part.replace("_llamaparse", "*")   # 카드 파일명의 llamaparse 유무 허용
    hits = glob.glob(os.path.join(CARDS, f"{book_glob}{page_part}*_v37.md"))
    if not hits:
        hits = glob.glob(os.path.join(CARDS, f"{book_glob}*_v37.md"))[:3]
    return sorted(hits)
```

여기에 `CARD_KEYWORDS["행정법"]`에 `"행정법강해"`, `["민법"]`에 `"컴민법"`, `["민사소송법"]`에 `"민소사례"`를 추가하면 과목단위 폴백에서 빠지는 77개 파일(행정법강해 47·컴민법 14·민소사례 7·포섭트리거 5·사례풀이순서 4)이 줄어든다. 포섭트리거·사례풀이순서는 과목명이 파일명에 있으므로 키워드 `포섭트리거_{과목}`·`사례풀이순서_{과목}`로 등록하면 된다. 행정법강해는 페이지 범위가 원문과 어긋나므로 논점단위 연결까지 하려면 **페이지 구간 겹침 기준 매칭**(원문 p241-270 ⊂ 카드 p239-275)이 추가로 필요하다.

### 4-2. [P1] 빌드 게이트 위반 후보: 번호 없는 cloze 17,085건 · 한자 잔존 14,636자

SKILL §9 빌드 전 확인 항목("한자 잔존 0", "번호 없는 cloze 0") 기준으로 스캔한 결과:

| 지표 | 건수 | 파일 수 | 상위 파일 |
|---|---|---|---|
| 번호 없는 cloze `{{…}}` | 17,085 | 143 | 헌법핵심정리300 p271-300 (434) · p091-120 (401) · 반반형법 p181-210 (313) · 사례풀이순서_형법각론 (306) · 신민사법선택형_가족법 p121-146 (283) · 논점민법재산법 p001-030 (281) |
| 한자 잔존 | 14,636자 | 242 | 사례풀이순서_민법 (1,367) · 포섭트리거_형법각론 (1,033) · 사례풀이순서_형법각론 (881) · 사례풀이순서_형법총론 (563) · 포섭트리거_형법총론 (527) |

- 번호 없는 cloze는 `{{헌법침해의 중대성}}`, `{{포괄흡수}}` 같은 실제 빈칸이다(정규식 오탐 아님, 표본 확인). 빌더가 자동 번호를 매기는지 이 저장소에서는 확인할 수 없다(`build_v37_apkg.py` 부재). **빌더 확인 전까지는 이 143파일을 apkg 빌드에 넣지 말 것.**
- 한자는 사례형 파생 카드(풀이순서·포섭트리거)에 집중돼 있다. 위키 쪽은 2026-07-12에 한자 교정(`wiki/사례_한자교정_2026-07-12/` 백업 2건)을 했지만 카드에는 반영되지 않았다. 甲乙丙丁 등 당사자 표기 허용 여부는 #37 규칙 원문을 확인한 뒤 일괄 처리할 것. 참고로 교정 후 위키 노트에 `乙`이 `Z`로 바뀐 사례가 있다(`wiki/사례/민법/채권질권_제3채무자_지위_승계.md` 20행) — 카드로 재추출하기 전에 위키 쪽 오교정부터 점검해야 한다.

### 4-3. [P2] 포맷·메타데이터 이질성

| 항목 | 현황 | 영향 |
|---|---|---|
| 파일 포맷 | `###`+앞/뒤 266 · `###`(빈칸만) 63 · **`##` 구포맷 53** · **비표준 라벨(`**앞:**`, `앞/뒤:`) 15** · 스텁 11 · 감사대장 1 | 빌더 파서가 `##` 구분자·볼드 라벨을 처리하는지 확인 필요. 6월 14~15일 생성분(신민사법선택형·논점민소·김기용형총)에 집중 |
| frontmatter | 없음 90파일. `과목:` 키 있는 파일 107/409 | build_session 과목 분류는 파일명 키워드 1차라 대부분 커버되지만, 키워드 미등록 파일(§4-1)은 frontmatter까지 없으면 미분류 |
| `과목:` 값 | `민사` 11 · `공법` 4 · `헌법 — 통치구조(…)` 3 · `형법` 19 | #50-B 풀네임 규칙 위반. `_SUBJ_NORM`이 민사·공법·형법은 흡수하지만 `헌법 — 통치구조`는 미흡수 |
| 태그 축 | `단계::` 9,085건/130파일 · `증명책임::` 261건/58파일 · `주제::` 10,078건/129파일 | 역할① A카드 필수축(`단계::`)이 전체 파일의 1/3에만 있음. 드릴 "#51 A 카드 우선 출제"가 이 파일들에 편중됨 |
| `검증필요` | 46건/8파일 (찌라시_상법 38) | 채점 제외 대상(daily-drill 룰 #13). 찌라시_상법은 사실상 전체가 미검증 |
| `기출` 표기 | 55건/23파일 (유니온헌법기출 49) | 기출문제집 소스라 출처 명시 조건 충족으로 보임. 나머지 4파일(민사사례연습1·민소사례·쟁점노트_소송집행·해커스헌법사례) 6건은 소스 명시 확인 필요 |
| `[불명확]` | 1건 (컴민법 p271-300) | 규칙대로 표기됨 |

### 4-4. [P2] 사례노트 ↔ 풀이순서 카드 불일치

| 과목(노트 frontmatter) | 사례노트 | `## 풀이순서` 보유 | 풀이순서 카드 | 차이 |
|---|---|---|---|---|
| 민법 | 410 | 410 | 384 | −26 |
| 민사소송법 | 200 | 194 | 127 | **−67** (풀이순서 섹션 없는 노트 6건 포함) |
| 형법각론 | 120 | 120 | 145 | +25 |
| 형법총론 | 69 | 69 | 80 | +11 |
| 헌법 | 58 | 58 | 38 | −20 |
| 형법 (비표준 과목값) | 26 | 26 | (파일 없음) | 형법각론 19·형법총론 7 폴더에 있음 → 위 +25/+11이 이 26건일 가능성 |

SKILL §4.2 "1사례 노트 = 1풀이순서 카드" 기준으로 민사소송법·헌법·민법 순으로 약 110건이 미카드화이거나 카드 제목이 노트와 어긋나 있다. `풀이순서검증` 마커는 118/883만 있어, 사례노트 쪽 게이트도 미완이다.

### 4-5. [P2] SRS→카드 핸드오프 경로 미가동 · 상태 파일 오염

- SRS 11건 전부 `repetitions 0`, `interval 1`, `next_review 2026-07-07`. `--srs-scores`로 채점을 넘긴 적이 없어 SM-2가 한 번도 돌지 않았다(daily-drill SKILL Phase 3 경고 그대로).
- 따라서 `srs_graduated_queue.jsonl`이 생성된 적 없고, `consume_graduated.py`도 실행된 적 없으며, `cards/약점포섭_*_v37.md` 증분 덱 파일도 없다. 핸드오프 코드는 존재하지만 **end-to-end 검증은 안 된 상태**로 보는 것이 맞다.
- `state/srs_events.jsonl`에 테스트 이벤트 2종(`임시테스트_결선검증` id 12, `테스트 쟁점 A` id 1)이 남아 있다. `learning.json`에는 반영돼 있지 않으나 이벤트 로그는 정리 대상.
- `learning.json topics`(민법서론_OT 등 1월 기록)와 `current_scope: 권리주체_행위능력`은 v1 시절 잔재로 현행 논점 키(`과목|대분류|논점`)와 형식이 다르다.

### 4-6. [P3] 문서·수치 불일치

- README·커밋 메시지 "카드 408개" ↔ 실제 409개.
- MOC(`wiki/_MOC.md`)의 "민법 132 논점 ✅"는 일치. 행정법 234·형법각론 110·헌법 106은 MOC에 수치가 없어 목차 대비 누락 검증 불가(MOC 갱신일 2026-06-26).

---

## 5. 다음 담당자 작업 목록 (2026-09-06 갱신 — 남은 일)

| 순서 | 작업 | 어디서 | 완료 기준 |
|---|---|---|---|
| 1 | **라이브 볼트에 매칭 패치 이식**: 이 저장소 `skills/daily-drill/scripts/build_session.py`의 `_원문_to_card_paths()`·`CARD_KEYWORDS` 변경분을 볼트 `.agent/skills/daily-drill/scripts/build_session.py`에 반영 | 볼트 | `build_session.py --subject 행정법` 브리프에 `카드[논점단위]` 표기 |
| 2 | **빌더 파서 확인**(`build_v37_apkg.py`): ① `##` 구포맷 22파일의 `앞:` 블록 분할 방식 ② 볼드 제거 후 라벨 인식 ③ cloze 번호 처리 | 볼트 | 확인 결과를 anki-card-generation SKILL §9에 1줄 명기 |
| 3 | 잔여 한자 889자(독음 병기형) 보존/변환 결정. 위키 甲 없는 2파일의 `Z` 20건 판단 | `cards/`·`wiki/사례/` | 결정 기록 |
| 4 | 민사소송법 사례노트 6건에 `## 풀이순서` 섹션 작성 → `python tools/gen_solving_order_cards.py` 재실행 | `wiki/사례/민사소송법/` | 카드 수 = 섹션 보유 노트 수 |
| 5 | SRS 연체 11건: 내신_2-2(10-19) **D-43**, 시험모드 자동 가동(D-21) 임박. 첫 드릴 마감 시 `--srs-scores` 반드시 전달 | daily-drill Phase 3 | `srs_log.json` `repetitions ≥ 1` |
| 6 | **카드 정규화·보충을 볼트 정본에 반영**: 저장소 `cards/`를 `outputs/02_cards_v37/`로 복사하거나, 볼트에서 `python tools/normalize_cards.py --root E:\법학볼트 --cards outputs/02_cards_v37` 및 `python tools/gen_solving_order_cards.py --root E:\법학볼트 --wiki sync/위키 --cards outputs/02_cards_v37` 실행(둘 다 멱등·`--dry-run` 지원) | 볼트 | `card_inventory.py --root E:\법학볼트 …` 지표가 §0과 일치 |
| 7 | 정규화된 카드로 apkg 재빌드 시 note_key(`{파일stem}::{basic|cloze}::{seq}`) 안정성 확인 — 헤딩 `##`→`###` 변환으로 seq가 바뀌지 않는지 | 볼트 빌더 | 재임포트 후 복습 이력 보존 |

**하지 말 것**: 정본 카드파일 재생성·전체 재빌드(SKILL §7-B "누락분만 보충, 전체 재생성 금지"), 스텁 11파일 삭제(게이트 근거), `.apkg`·개인 학습 상태 커밋(SKILL §8).

---

## 6. 재현 방법

```bash
# 저장소 스냅샷 기준
python tools/card_inventory.py

# 라이브 볼트 기준 (경로 인자만 바꾸면 동일 스크립트 사용)
python tools/card_inventory.py --root "E:\법학볼트" --cards outputs/02_cards_v37 --wiki sync/위키 --state .agent/state
```

출력: 책별 커버리지 표, 포맷 분류, 품질 지표(cloze·한자·태그축·검증필요), `과목:` 값 분포, 위키 카드검증 게이트, 사례노트↔풀이순서 대조, SRS 핸드오프 상태.

수정 스크립트(모두 표준 라이브러리, 멱등, `--dry-run` 지원):

```bash
python tools/normalize_cards.py --dry-run          # cloze 번호·한자·구분자·라벨·과목 정규화
python tools/gen_solving_order_cards.py --dry-run  # 사례노트 풀이순서 → 역할② 카드 보충
```

---

## 7. 참조 파일 맵

| 목적 | 경로 |
|---|---|
| 카드 규칙 (3역할·태그축·누락0 게이트·파일명 규약·apkg 빌드 체크) | `skills/anki-card-generation/SKILL.md` |
| 카드 → 드릴 출제 소스 연결 | `skills/daily-drill/scripts/build_session.py` 286~360행, SKILL "소스(카드 정본, #51)" |
| SRS 졸업 → 카드 핸드오프 | `skills/spaced-repetition/scripts/srs_scheduler.py` 550~582행 → `skills/anki-card-generation/scripts/consume_graduated.py` |
| 논점 정본 (카드검증·카드감사 마커) | `wiki/{과목}/*.md` frontmatter |
| 사례 정본 (풀이순서 섹션) | `wiki/사례/{과목}/*.md` |
| 학습 상태 | `state/learning.json` (약점·SRS·시험) · `state/srs_log.json` · `state/srs_events.jsonl` · `state/drill_log.jsonl` |
| 이전 현황·재개 지점 (볼트, 이 저장소에 없음) | `9.작업중/클로드/현황_진도보드_웹앱화_git_앱_2026-07-14.md` |
